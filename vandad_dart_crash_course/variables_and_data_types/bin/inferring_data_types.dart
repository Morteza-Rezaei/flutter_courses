void main(List<String> args) {
  // in here we tell the data type of the variable explicitly
  const int age1 = 20;

  // in here we let the dart infer the data type of the variable
  const age2 = 20;

  print(age1);
  print(age2);
}
